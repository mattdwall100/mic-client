// Template for local endpoint overrides. Copy to `config.js` and edit for your network.
// (config.js is git-ignored so machine-specific addresses aren't committed.)
window.ALFRED_CONFIG = {
  api: "http://localhost:8000", // local-assistant-server
  lifecycle: "http://localhost:9000", // server-lifecycle-manager
};
